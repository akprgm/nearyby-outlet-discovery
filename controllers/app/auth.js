var mongoose = require('mongoose');
var utility = require('../utility');
module.exports = {
    login: function login(){//method for login functionality in our mobile app
        userInfo();
    },
    register: function register(){//method for registering new user

    }
}